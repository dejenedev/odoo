<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_pki_approval_rule_form" model="ir.ui.view">
        <field name="name">pki.approval.rule.form</field>
        <field name="model">pki.approval.rule</field>
        <field name="arch" type="xml">
            <form string="Approval Rule">
                <sheet>
                    <group>
                        <group>
                            <field name="name"/>
                            <field name="model_id"
                                   options="{'no_create': True}"/>
                            <field name="sequence"/>
                        </group>
                        <group>
                            <field name="group_id"
                                   options="{'no_create': True}"/>
                            <field name="min_amount"/>
                            <field name="company_id"/>
                            <field name="active"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Fields to Hash">
                            <field name="field_ids" widget="many2many_tags"
                                   options="{'no_create': True}"
                                   domain="[('model_id', '=', model_id), ('store', '=', True), ('ttype', 'not in', ('binary', 'one2many', 'many2many'))]"/>
                            <div class="text-muted mt-2">
                                Select the document fields whose values will be included in the
                                cryptographic hash. Changes to these fields after signing will be
                                detected as tampering. If none selected, default business fields are used.
                            </div>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_pki_approval_rule_list" model="ir.ui.view">
        <field name="name">pki.approval.rule.list</field>
        <field name="model">pki.approval.rule</field>
        <field name="arch" type="xml">
            <list string="Approval Rules">
                <field name="sequence" widget="handle"/>
                <field name="name"/>
                <field name="model_id"/>
                <field name="group_id"/>
                <field name="min_amount"/>
                <field name="company_id"/>
            </list>
        </field>
    </record>

    <record id="view_pki_approval_rule_search" model="ir.ui.view">
        <field name="name">pki.approval.rule.search</field>
        <field name="model">pki.approval.rule</field>
        <field name="arch" type="xml">
            <search string="Approval Rules">
                <field name="name"/>
                <field name="model_id"/>
                <field name="group_id"/>
                <group>
                    <filter string="Document Type" name="group_model"
                            domain="[]" context="{'group_by': 'model_id'}"/>
                </group>
            </search>
        </field>
    </record>

    <record id="action_pki_approval_rule" model="ir.actions.act_window">
        <field name="name">Approval Rules</field>
        <field name="res_model">pki.approval.rule</field>
        <field name="view_mode">list,form</field>
        <field name="search_view_id" ref="view_pki_approval_rule_search"/>
    </record>

</odoo>
